<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
</head>
<style>
 body {
    font-family: 'Poppins', sans-serif;
    text-align: center;
    background-color: #f8f1e4;
    margin: 0;
    padding: 20px;
}

h1 {
    color: #6d4c41;
    font-size: 30px;
    margin-bottom: 20px;
}

/* Form Styles */
form {
    background: #ffffff;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    display: inline-block;
}

form table {
    width: 100%;
}

form td {
    font-size: 18px;
    color: #6d4c41;
    padding: 10px;
}

input[type="text"],
input[type="number"] {
    font-size: 18px;
    padding: 8px;
    width: 100%;
    border: 1px solid #ccc;
    border-radius: 5px;
}

input[type="submit"] {
    background: #8d6e63;
    color: white;
    font-size: 20px;
    border: none;
    padding: 10px 20px;
    cursor: pointer;
    border-radius: 5px;
    transition: 0.3s;
}

input[type="submit"]:hover {
    background: #6d4c41;
}

</style>
<body>
    <div align="center">
        <h1>Edit Book</h1>
        <form action="/edit/<%= book.id %>" method="post">
            <table>
                <tr>
                    <td>Title:</td>
                    <td><input type="text" name="title" value="<%= book.title %>" required></td>
                </tr>
                <tr>
                    <td>Author:</td>
                    <td><input type="text" name="author" value="<%= book.author %>" required></td>
                </tr>
                <tr>
                    <td>Country</td>
                    <td><input type="text" name="country"value="<%= book.country %>" required /></td>
                </tr>
                <tr>
                    <td>Language</td>
                    <td><input type="text" name="language"value="<%= book.language %>"  required /></td>
                </tr>
                <tr>
                    <td>Edition</td>
                    <td><input type="text" name="edition"value="<%= book.edition %>"  required /></td>
                </tr>
                <tr>
                    <td>Pages</td>
                    <td><input type="text" name="pages" value="<%= book.pages %>"  required /></td>
                </tr>
                <tr>
                    <td>Genre:</td>
                    <td><input type="text" name="genre" value="<%= book.genre %>" required></td>
                </tr>
                <tr>
                    <td>Published Year:</td>
                    <td><input type="number" name="publishedYear" value="<%= book.publishedYear %>" required></td>
                </tr>
                <tr>
                    <td></td>
                    <td><input type="submit" value="Update Book"></td>
                </tr>
            </table>
        </form>
    </div>
</body>
</html>

